export const metadata = {
  title: "Fresh Fruit Juice — Menu",
  description: "Fresh pressed fruit juices — digital menu",
};

export default function JuiceLayout({ children }) {
  return children;
}
